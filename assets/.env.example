# Example debug-only local override for Flutter web. Copy to a root-level
# `.env` (gitignored) to use it. Contains no secrets by design — release
# builds ignore `.env` entirely (see AppConfig.serverHost / lib/bootstrap.dart).
SERVER_HOST=http://localhost:8007
