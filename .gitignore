# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.env
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Secrets
backend/.env
# Upload keystore config — local path + plaintext passwords; CI reconstructs from
# GitHub secrets (see docs/RELEASE_SIGNING.md).
key.properties
android/key.properties
# Keystores under the android/ tree (the keystore itself lives outside the repo,
# but belt-and-braces in case anyone drops one in).
android/**/*.jks
android/**/*.keystore

# Local tooling
.vscode/

# Databases
backend/*.db
backend/*.sqlite3
backend/swasth.db

# pytest artifacts
backend/.coverage

# Backup directories
ios_backup/

# Claude Code — per-developer state (never push, still pull team changes)
.claude/settings.local.json
.claude/sessions/
.claude/statusline-state/
.claude/markers/
.claude/compact-state.md

# Personal operational docs — local-only (see memory: project_india_trip_2026)
docs/INDIA_TRIP_APR_MAY_2026.md
docs/FOUNDER_KIT_CHECKLIST.md
