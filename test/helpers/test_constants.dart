// Shared test-only constants. Import here rather than scattering literals
// so the mock source and assertions always stay in sync.
abstract final class TestConstants {
  static const String mockToken = 'mock_token_123';
}
